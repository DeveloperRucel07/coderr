from django.apps import AppConfig


class CoderrAppConfig(AppConfig):
    """
    Configuration for the coderr_app Django application.

    This app handles offers, orders, and reviews for a marketplace platform.
    """
    name = 'coderr_app'
